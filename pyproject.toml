[project]
name = "ms-mail-automation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "msal>=1.35.0",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "selenium>=4.41.0",
]
